<table class="text-center table table-bordered border-primary col-md-5  top-40 w-50 mx-auto my-5">

  <thead>
    <tr>
      <th scope="col　">名前</th>
      <th scope="col"></th>
      
    </tr>
  </thead>
  <tbody>
    <% users.each do |user| %>
        <tr>
        <th scope="row"><%= user.name%></th>
        <td><%= link_to "削除", del_user_path(user), method: :post,data: { confirm: "You sure?" }, style: "text-decoration: none; color: red;" ,remote: true%>
</td>
        </tr>
    <% end %>
    
  </tbody>
</table>